import cv2
import numpy as np
img1=cv2.imread("New folder/img6 (1).png")
img2=cv2.imread("New folder/img6 (3).png")
img1=cv2.resize(img1,(400,400))
img2=cv2.resize(img2,(400,400))
#new_img=cv2.addWeighted(img1,1.5,img2,0.5,1)
#new_img=cv2.subtract(img1,img2)
#new=cv2.bitwise_and(img1,img2)
#h=np.hstack((img1,img2,new))
# new=cv2.bitwise_or(img2,img1)
 #h=np.hstack((img1,img2,new))
#new=cv2.bitwise_xor(img2,img1)
 #h=np.hstack((img1,img2,new))
#new=cv2.bitwise_not(img2)
 # new1=cv2.bitwise_not(img1)
 #h=np.hstack((img1,new))
 # h1=np.hstack((img2,new1))
#r=np.vstack((h,h1))
new=cv2.Canny(img2,100,100,apertureSize=3,L2gradient=True)
cv2.imshow('zawn',new)
cv2.waitKey(0)
cv2.destroyAllWindows()

